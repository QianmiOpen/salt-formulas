{%- from 'logstash/settings.sls' import logstash with context %}
input {

  ##catlina.out
  redis {
    host => "{{ logstash.redisIP }}" # string (optional), default: "127.0.0.1"
    port => {{ logstash.redisPort }} # number (optional), default: 6379
    batch_count => 100 # number (optional), default: 1
    data_type => "list" # string, one of ["list", "channel", "pattern_channel"] (optional)
    db => 0 # number (optional), default: 0
    key => "LOGSTASH_CATALINA" # string (optional)
    type => "catalina" # string (optional)
  }

  ##interface log
  redis {
    host => "{{ logstash.redisIP }}" # string (optional), default: "127.0.0.1"
    port => {{ logstash.redisPort }} # number (optional), default: 6379
    batch_count => 100 # number (optional), default: 1
    data_type => "list" # string, one of ["list", "channel", "pattern_channel"] (optional)
    db => 0 # number (optional), default: 0
    key => "LOGSTASH_INTF_LOG" # string (optional)
    type => "intflog" # string (optional)
  }

  ##error log
  redis {
    host => "{{ logstash.redisIP }}" # string (optional), default: "127.0.0.1"
    port => {{ logstash.redisPort }} # number (optional), default: 6379
    batch_count => 100 # number (optional), default: 1
    data_type => "list" # string, one of ["list", "channel", "pattern_channel"] (optional)
    db => 0 # number (optional), default: 0
    key => "LOGSTASH_ERR_LOG" # string (optional)
    type => "errlog" # string (optional)
  }

  ##srv log
  redis {
    host => "{{ logstash.redisIP }}" # string (optional), default: "127.0.0.1"
    port => {{ logstash.redisPort }} # number (optional), default: 6379
    batch_count => 100 # number (optional), default: 1
    data_type => "list" # string, one of ["list", "channel", "pattern_channel"] (optional)
    db => 0 # number (optional), default: 0
    key => "LOGSTASH_SRV_LOG" # string (optional)
    type => "srvlog" # string (optional)
  }

  ##app log
  redis {
    host => "{{ logstash.redisIP }}" # string (optional), default: "127.0.0.1"
    port => {{ logstash.redisPort }} # number (optional), default: 6379
    batch_count => 100 # number (optional), default: 1
    data_type => "list" # string, one of ["list", "channel", "pattern_channel"] (optional)
    db => 0 # number (optional), default: 0
    key => "LOGSTASH_APP_LOG" # string (optional)
    type => "applog" # string (optional)
  }
}
